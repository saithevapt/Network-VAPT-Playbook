A relational database management system used to store and manage application data.
Port Number:3306

msfconsole -q
use auxiliary/scanner/mysql/mysql_version
set RHOST ip
run
#Identifies the MySQL database version running on the target system

use auxiliary/admin/mysql/mysql_enum
set RHOST ip
set USERNAME Name
SET PASSWORD pass
Run
#Enumerates MySQL users, databases, and privileges using valid credentials.
